# OpenClaw skills

The version-controlled YouTube skills in `openclaw/skills/` use the supported
loopback `ytctl` boundary. To install or refresh them locally, synchronize each
directory into `~/.openclaw/workspace/skills/`, preserving `SKILL.md` and
removing obsolete scripts from earlier versions.

The installed runtime copy on the Mac mini is validated during T077 rollout.
